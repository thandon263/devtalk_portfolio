
<div class="album py-5 bg-light">
    <div class="container">
        <div class="row">
            <div class="col-md-7"><%= link_to "Create New Item", new_portfolio_path if logged_in?(:site_admin) %></div>
            <hr>
        </div>

        <div class="row">
            <%= render partial: 'portfolio_item', collection: @portfolio_items %>
        </div>
    </div>
</div>