<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag    'blogs', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <div class="container">
    <%# Start of header %>
     <header class="blog-header py-3">
      <div class="container">
          <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="col-4 pt-1"></div>
            <div class="col-4 text-center">
              <a class="blog-header-logo text-dark" href="#">Dev360 Groupe</a>
            </div>
            <div class="col-4 d-flex justify-content-end align-items-center">
            </div>
          </div>
        </div>
      </header> 
    <%# End of Header %>

      <%= render 'shared/blog_nav' %>

      <%# Start of heading %>
      <div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
        <div class="col-md-9 px-0">
          <h1 class="display-4 font-italic">Welcome To Dev360 Blog Introduction</h1>
          <p class="lead my-3">We crave the new. We fear the new. But most of all, we need the new. There's even a word for it: neophilia. With the social and technological change creating competing visions of the future.</p>
          <p class="lead mb-0"><%= link_to "Write a New Blog Post...", new_blog_url if logged_in?(:site_admin) %></p>
        </div>
      </div>
      <%# End of heading %>

      <%# Start of container and row element %>
        <main role="main" class="container">
          <div class="row">
      <%# End of container and row element %>

            <%= yield %>

            <%= render "shared/blog_sidebar" %>

          </div>

      <%# Start of Footer %>
             <footer class="blog-footer">
                <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
                <p>
                  <a href="#">Back to top</a>
                </p>
              </footer>
      <%# End of footer %>

            <%= render "shared/nav", location: "bottom" %>
            
            <%= source_helper("blog") %>
        </main>
      </div>
  </body>
</html>
